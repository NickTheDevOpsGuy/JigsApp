/**
 * Completion overlay: no confetti (glow/celebration only).
 */
import { useEffect } from "react";

export function useCompletionConfetti(): void {
  useEffect(() => {
    /* Confetti disabled; glow/UI is used for celebration. */
  }, []);
}
