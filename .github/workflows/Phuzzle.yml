# .github/workflows/Phuzzle.yml
name: 🧩 Phuzzle

on:
  pull_request:
    branches:
      - develop

concurrency:
  group: Phuzzle-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint-typecheck:
    name: 🧩 Phuzzle
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    permissions:
      contents: read
      pull-requests: read
      statuses: write # for Lighthouse CI status checks (if enabled later)

    defaults:
      run:
        working-directory: .

    steps:
      - name: ⬇️ Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: 🧰 Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: "lts/*"
          cache: npm
          cache-dependency-path: package-lock.json

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🧱 Quality guards (no test.skip / no console.log in src)
        run: npm run guard:quality

      - name: 🧹 ESLint
        run: npm run lint

      - name: 🧪 TypeScript typecheck
        run: npm run typecheck

      - name: 🎨 Prettier check
        run: npm run format:check

      - name: 🏗️ Build
        run: npm run build

      - name: 📦 Bundle size check
        env:
          BASELINE_REF: origin/${{ github.base_ref }}
        run: npm run check:bundle

      - name: 🧪 Unit tests (Vitest)
        run: npm run test

      - name: 📦 Install Playwright browsers
        run: npx playwright install --with-deps chromium msedge

      - name: 🎭 E2E tests (Playwright)
        run: |
          npx playwright test --project=chromium --project=chromium-tz-la --project=chromium-tz-auckland
          npx playwright test --project=msedge src/app/screens/Play/PlayScreen.completion-menu-edge.e2e.spec.ts

      - name: 🔦 Lighthouse CI (perf, a11y)
        env:
          LHCI_UPLOAD__GITHUB_TOKEN: ${{ github.token }}
        run: npx lhci autorun
